#include <vector>
#include "poker.h"

#define POKER_SIZE 13

using namespace std;

//extern int compare(vector<vector<Poker>> &);
vector<int> compare(vector<vector<Poker>>& );
extern void test();
//extern void deal(Poker &);
extern void deal(vector<vector<Poker>>&);